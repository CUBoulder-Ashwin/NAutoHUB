{% extends "base.html" %}

{% block title %}DIFF{% endblock %}

{% block content %}
<div style="text-align: center; margin-top: 100px">
    <h1>Config Difference Comparator</h1>
<div class="form-container">
    <form action="/diff" method="post">
        <div class="form-row">
            <text>Enter device name:</text>
            <input 
            type="text" 
            placeholder="Device name" 
            name="device"
            />
        </div>
        <div class="form-row">
            <text>Enter an IP address on any interface on the device:</text>
            <input 
            type="text" 
            placeholder="Interface IP" 
            name="IPv4"
            />
        </div>
    </div>
        <div class="button-container">
            <button type="submit" class="btn btn-secondary">Compare Config</button>
        </div>
    </form>
    {% if m %}
        <h3 id="file" style="color: green; padding-top: 20px;">{{m}}</h3>
    {% endif %}

    <script>
        function hideElementAfterTimeout(elementId, timeout) {
            setTimeout(function() {
                var element = document.getElementById(elementId);
                if (element) {
                    element.style.display = 'none';
                }
            }, timeout);
        }
        hideElementAfterTimeout('file', 60000); 

    </script>
</div>
{% endblock %}

